spring:
  application:
    name: donor-client

server:
  port: 2222
  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka